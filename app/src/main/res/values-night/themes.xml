<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Colores de superficie y fondo -->
        <item name="colorSurface">@color/surface_dark</item>
        <item name="android:colorBackground">@color/black</item>

        <!-- Colores de texto -->
        <item name="colorOnSurface">@color/white</item>
        <!--<item name="colorOnSurfaceVariant">@color/gray_input_texto_dark</item>-->
        <item name="colorOnSurfaceVariant">@color/greySecondary</item>


        <!-- Color primario -->
        <item name="colorPrimary">@color/primaryViolet</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Colores de controles -->
        <item name="colorOutline">#49454F</item>

        <!-- Atributos personalizados -->
        <item name="inputBackground">@color/gray_input_dark</item>
        <item name="btnCloseBackground">@color/fondoBtnClose_dark</item>
        <item name="backgroundTint">@color/greyBackground_dark</item>
        <item name="iconTint">#FFFFFF</item>
        <item name="bottomNavSelectedColor">@color/primaryViolet</item>
        <item name="bottomNavUnselectedColor">#B0B0B0</item>
    </style>
</resources>